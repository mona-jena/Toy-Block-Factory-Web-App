---
steps:
  - label: 'do not panic :cherry_blossom::whale:'
    command: 'docker build -f ./Dockerfile.test .'
    agents:
      queue: grace-lab
  - label: 'mona second step'
    command:
      - 'docker build -t tbf -f ./Dockerfile .'
    agents:
      queue: grace-lab
