---
steps:
  - label: ':cherry_blossom::whale: test panic'
    command: 'docker build -f ./Dockerfile.test .' #why don't we run the tests?
    agents:
      queue: grace-lab
  - wait 
  - label: ':ec2: make dockerimage with tbf'
    command:
      - 'docker build -t 138666658526.dkr.ecr.ap-southeast-2.amazonaws.com/mona-app:${BUILDKITE_BUILD_NUMBER} .' #is it finding app in current dir of VM?
      - 'docker push 138666658526.dkr.ecr.ap-southeast-2.amazonaws.com/mona-app:${BUILDKITE_BUILD_NUMBER}'
    plugins:
      - ecr#v2.3.0:
          login: true
          account_ids: "138666658526"
          region: "ap-southeast-2"
    agents:
      queue: grace-lab 
  - wait
  - label: ':k8s: deploy on jupiter'
    command: 'ktmpl deployment.yml -p imagetag ${BUILDKITE_BUILD_NUMBER} | kubectl apply -f - -n fma'  # EXPLAIN!!!
    agents:
      queue: europa-preprod-fma

  - block
  - label: 'make dockerimage for web app'
    command: 
      - 'docker build -f ./Dockerfile.web . -t webapp'
      - 'docker run -d -p 3000:3000 webapp' ##what's source port? #why do you need to run on detached mode?
    agents:
      queue: grace-lab
