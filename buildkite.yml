---
steps:
  - label: 'do not panic :cherry_blossom::whale:'
    command: 'docker build -f ./App/Dockerfile.test .'
    agents:
      queue: grace-lab
  - label: 'mona second step'
    command:
      - 'docker build -t tbf -f ./App/Dockerfile .'
    agents:
      queue: grace-lab
